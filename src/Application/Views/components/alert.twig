{% if hasFlash('infos') %}
    <div class="alert alert-warning alert-dismissible fade show mt-5 pt-3" role="alert">
       <ul>
           {% for info in flash('infos') %}
               <li>{{ info }}</li>
           {% endfor %}
       </ul>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="{{ 'CloseAriaLabel'|trans|raw }}"></button>
    </div>
{% endif %}

{% if hasFlash('errors') %}
    <div class="alert alert-danger alert-dismissible fade show mt-5 pt-3" role="alert">
        <ul>
            {% for error in flash('errors') %}
                <li>{{ error }}</li>
            {% endfor %}
        </ul>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="{{ 'CloseAriaLabel'|trans|raw }}"></button>
    </div>
{% endif %}

{% if hasFlash('success') %}
    <div class="alert alert-success alert-dismissible fade show mt-5 pt-3" role="alert">
        <ul>
            {% for success in flash('success') %}
                <li>{{ success }}</li>
            {% endfor %}
        </ul>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="{{ 'CloseAriaLabel'|trans|raw }}"></button>
    </div>
{% endif %}